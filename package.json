{
  "name": "mre-gatekeeper",
  "version": "0.0.1",
  "description": "MRE Access manager",
  "main": "build/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx rimraf ./build && npx tsc",
    "start": "npm run build && node build/server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/line-reader": "0.0.29",
    "@types/restify": "^8.4.2",
    "@types/semver": "^7.2.0",
    "@types/ws": "^7.2.4",
    "eslint": "^7.2.0",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "@microsoft/mixed-reality-extension-sdk": "^0.18.1",
    "dotenv": "^8.2.0",
    "line-reader": "^0.4.0"
  }
}
